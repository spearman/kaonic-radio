[package]
name = "kaonic-factory"
version = "0.1.0"
edition = "2021"
rust-version = "1.75"
description = "Factory Service for Kaonic"
repository = "https://github.com/BeechatNetworkSystemsLtd/kaonic-radio"

[dependencies]

# Kaonic
radio-rf215 = { path="../radio-rf215/" }
kaonic-radio = { path="../kaonic-radio/" }

# Logging
simple_logger = "=5.0.0"
log = "0.4"

# Protobuf and gRPC
tonic = "=0.13.0"
prost = "=0.13.5"
tokio = { version = "=1.48.0", features = ["macros", "rt-multi-thread"] }
tokio-stream = { version = "=0.1.16", features = ["sync"] }

[build-dependencies]
tonic-build = "=0.13.0"

